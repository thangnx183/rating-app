import React from 'react';

export default class rating extends React.Component{

    render(){
        return(
            <div>
                u can rate ur study here;
            </div>
        )
    }
}