import React from 'react';

export default class Request extends React.Component{

    render(){
        return(
            <div>
                u can see request of all ur classmates's rating;
            </div>
        )
    }
}